<div class="col col-work">
            <% if @works1.count != 0%>
            <div class="col-lg-4 ">
                <div class="category mb-30">
                        <div class="list-title">
                            <h4 class= "font-weight-bold text-center"> Upcoming Work </h1>
                        </div>
                </div>
            </div>
            <% end %>
            <div class="row">
            <% @works1.each do |work| %>
                <div class="col-lg-4 ">
                    <div class="category mb-30 todolist-card">
                        <div class="job">
                            <% if work.priority == "High"%>
                                <span class="colors5 mb-4">High</span>
                            <% elsif work.priority == "Medium" %>
                                <span class="colors1 mb-4">Medium</span>
                            <% else %>
                                <span class="colors2 mb-4">Low</span>
                            <% end %>
                            <h5 class= "font-weight-bold"><%= work.work%> </h5>
                            <ul class="place">
                            <li>
                                <p><i class="fas fa-map-marker-alt pe-2"></i> <%= work.status %></p>
                            </li>
                            </ul>
                            <div class="pricing d-flex justify-content-between align-items-center">
                                <div class="left">
                                    <p>Created <%= time_ago_in_words(work.created_at) %></p>
                                    <td>
                                        <%= link_to "Show", todolist_path(work), class:"btn btn-primary font-weight-bold"%>
                                        <%= link_to "Edit", edit_todolist_path(work), class:"btn btn-info font-weight-bold"%>
                                        <%= link_to "In-progress", todolist_path(work, status: "In-progress"), class:"btn btn-warning font-weight-bold"%>
                                    </td>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                </div>
            <% end %>
            </div>
</div>